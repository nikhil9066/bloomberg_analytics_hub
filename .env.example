# =============================================================================
# Bloomberg API Configuration (OPTIONAL - Only for Data Ingestion Pipeline)
# =============================================================================
# These credentials are NOT required for the dashboard to function.
# Only set these if you're running the separate data ingestion pipeline
# that fetches data from Bloomberg API and pushes it to HANA.
#
# For dashboard-only deployment, you can leave these commented out or empty.
# =============================================================================
# BLOOMBERG_CLIENT_ID=your_bloomberg_client_id_here
# BLOOMBERG_CLIENT_SECRET=your_bloomberg_client_secret_here
# BLOOMBERG_API_HOST=https://api.bloomberg.com
# BLOOMBERG_OAUTH_ENDPOINT=https://bsso.blpprofessional.com/ext/api/as/token.oauth2

# =============================================================================
# SAP HANA Cloud Configuration (REQUIRED for Dashboard)
# =============================================================================
# These credentials are REQUIRED for the dashboard to pull and display data.
# Make sure these are set correctly for your HANA Cloud instance.
# =============================================================================
HANA_ADDRESS=your-hana-instance.hana.prod-us10.hanacloud.ondemand.com
HANA_PORT=443
HANA_USER=DBADMIN
HANA_PASSWORD=your_hana_password_here
HANA_SCHEMA=BLOOMBERG_DATA
HANA_TABLE=FINANCIAL_RATIOS

# File Paths
DATA_DIR=data
DOWNLOADS_DIR=downloads
IDENTIFIERS_FILE=data/identifiers.json

# Application Configuration
PORT=8080
DASH_DEBUG=false
FLASK_ENV=production
