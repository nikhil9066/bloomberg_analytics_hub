# =============================================================================
# SAP HANA Cloud Configuration (REQUIRED)
# =============================================================================
HANA_ADDRESS=your-hana-instance.hana.prod-us10.hanacloud.ondemand.com
HANA_PORT=443
HANA_USER=DBADMIN
HANA_PASSWORD=your_hana_password_here
HANA_SCHEMA=BLOOMBERG_DATA
HANA_TABLE=FINANCIAL_RATIOS

# =============================================================================
# Email Configuration for Security Alerts (OPTIONAL)
# =============================================================================
# Used for sending notifications on failed login attempts via SendGrid API
# Leave blank to disable email notifications (will be logged only)

SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=your-verified-sender@example.com
NOTIFICATION_EMAIL=admin@example.com

# SendGrid Setup Notes:
# 1. Sign up at https://sendgrid.com (free tier: 100 emails/day)
# 2. Go to Settings > API Keys > Create API Key
# 3. Give it "Full Access" or at minimum "Mail Send" permissions
# 4. Copy the API key to SENDGRID_API_KEY above
# 5. Verify your sender email in SendGrid: Settings > Sender Authentication
# 6. NOTIFICATION_EMAIL receives all security alerts

# Application Configuration
PORT=8080
DASH_DEBUG=false
FLASK_ENV=production
